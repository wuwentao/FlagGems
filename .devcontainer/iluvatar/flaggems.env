# Iluvatar (IX) backend specific environment variables
# FLAGGEMS_BACKEND=IX corresponds to CMakeLists.txt: add_compile_definitions(FLAGGEMS_USE_IX)
FLAGGEMS_BACKEND=IX
CMAKE_ARGS="-DFLAGGEMS_BUILD_C_EXTENSIONS=ON -DCMAKE_BUILD_TYPE=Release -DFLAGGEMS_BACKEND=IX"
