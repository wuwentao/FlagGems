# Mthreads (Moore Threads) backend specific environment variables
# Mthreads uses MUSA backend in CMakeLists.txt: add_compile_definitions(FLAGGEMS_USE_MUSA)
FLAGGEMS_BACKEND=MUSA
CMAKE_ARGS="-DFLAGGEMS_BUILD_C_EXTENSIONS=ON -DCMAKE_BUILD_TYPE=Release -DFLAGGEMS_BACKEND=MUSA"
