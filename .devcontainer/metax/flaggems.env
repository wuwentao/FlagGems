# MetaX backend specific environment variables
# MetaX uses GCU backend in CMakeLists.txt: add_compile_definitions(FLAGGEMS_USE_GCU)
FLAGGEMS_BACKEND=GCU
CMAKE_ARGS="-DFLAGGEMS_BUILD_C_EXTENSIONS=ON -DCMAKE_BUILD_TYPE=Release -DFLAGGEMS_BACKEND=GCU"
